version: '3'
services:
  redirect-server:
    env_file:
      - ../.env
    volumes:
      - ../api:/app
      - /app/node_modules
    ports:
      - "${REDIRECT_SERVER_PORT}:${REDIRECT_SERVER_PORT}"
    networks:
      - harvest.dashboard
networks:
  harvest.dashboard:
    name: harvest.dashboard
    external: true
